# 食・掩蔽予報フェーズ実装計画

- 作成日: 2026-07-30
- 対象: Web版、macOS版
- 根拠: `docs/product/user-stories-events.md`
- 技術判断: `docs/decisions/0002-local-event-ephemeris.md`
- 状態: 主要Must実装済み・独立監査と軽量化を反復中

## 完成像

既存の星図は起動時の軽さと操作を維持する。利用者が探索領域を「現象」へ
切り替えると、現在の観測日時を起点に、現在地点での日食、月食、月による
明るい恒星掩蔽を端末内で検索できる。選択した現象には局地可視性、接触時刻、
高度・方位、精度条件、安全注意を示し、明示操作で星図時刻へ移動・復帰できる。

## 実装順

1. **共有暦と再現性**
   - DE442sのURL、MD5、SHA-256、coverageを固定する。
   - SSB→EMB、SSB→Sun、EMB→Moonの係数を5年chunkへFloat32で抽出する。
   - manifest、schema、`--check`、raw/gzip budget、全境界の検証を追加する。
   - WebとSwiftのClenshaw評価を共有fixtureで照合する。

2. **有限距離天体パイプライン**
   - UTC／TT／TDB／UT1、光行時間、観測者位置・速度、歳差章動、地球回転、
     極運動を一つのbody-state APIへまとめる。
   - 太陽・月の中心方向、距離、角半径、高度・方位を返す。
   - 既存星用モデルへ影響させず、月表示とイベント計算だけで遅延利用する。

3. **候補索引と求根**
   - 新月・満月の粗い候補を生成時に索引化する。
   - bracketを失わないsafeguarded root solverと極小探索を両言語へ実装する。
   - 日食C1〜C4、月食P1〜P4／U1〜U4、掩蔽の潜入・出現をsigned clearanceで解く。
   - 接線、二重根、日跨ぎ、chunk境界、対応期間端を共有fixture化する。

4. **Web縦切り**
   - `React.lazy`で現象パネルと計算コードを分離する。
   - 監査済み同一origin asset loader、LRU、再試行、AbortSignal、stale結果破棄を実装する。
   - 「恒星｜現象」、一覧、詳細、接触操作、復帰、空・エラー状態を統合する。
   - 日食の安全注意と精度条件を常設する。

5. **macOS縦切り**
   - resource loaderとactor予報serviceを実装する。
   - SwiftUIの現象一覧・詳細・接触操作を既存navigationへ統合する。
   - MainActorへ重い探索を入れず、取消と非アクティブ停止を実装する。

6. **精度・性能・利用体験の反復**
   - JPL SPK、NASA/GSFC、JPL Horizons等の独立基準と比較する。
   - 初期転送、初回現象表示、メモリ、30日・1年検索時間を予算化する。
   - 1280×720、390×844、200%表示、キーボード、reduced-motion、
     forced-colors、Canvas代替情報を確認する。
   - 一般、観望、天文、開発、アクセシビリティ、無関心の各Mustを再監査する。

## 多視点レビュー後の実施状況

初回の縦切り後に、一般利用者、観望者、天文利用者、開発者、
アクセシビリティ利用者、機能に関心がない利用者の各ストーリーを再評価した。
その結果、種類の絞り込み、観測地点の現地日付による年区切り、
地平線下だけにある現象からの復旧、物理的な境界不確かさ、初期転送量を
優先して追加した。

| 領域 | 状態 | 実施内容 |
| --- | --- | --- |
| 共有暦・候補 | 完了 | DE442s 41 chunk、1900〜2100年の10,720候補、hash・schema・再現検査 |
| 時刻・地球姿勢 | 完了 | UTC／TAI／TT／TDB／UT1、IERS EOP、範囲外ΔTと連続UTCシナリオ |
| 日食 | 完了 | 局地接触、最大、接触位置角、可視性、食分、遮蔽率、安全注意、±6 km実月縁境界 |
| 月食 | 完了 | 半影・部分・皆既接触、接触位置角、Danjon影半径、食分、局地可視性 |
| 恒星掩蔽 | 参考予報として完了 | BSC5P明るい星、潜入・最接近・出現、位置角、平均月縁、境界不確実性 |
| Web／Mac導線 | 完了 | 恒星／現象切替、年・種類・地平線フィルター、現地／UTC、接触時刻への移動と復帰 |
| 相対配置・任意時刻 | 完了 | 解いた接触・最大／最接近の正確な切替に加え、DE442s・EOP・観測地点・精密星表から指定UTCを毎回再計算するscrub／概要再生を両版へ実装。接触区間の物理sample gridと全solver時刻からイベント共通の角視野を作り、画面座標は補間しない |
| 次候補選択 | 完了 | 観測年は局地最大が観測日時以後の最初、全件過去は最新、別年は先頭 |
| 地点精度 | 完了 | 明示的な端末測位の水平精度を保持し、日食・掩蔽の保守的境界幅へ加算 |
| 年端coverage | 完了 | 光行時間reserve込みの安全区間を共有fixture化し、1900／2100年の現地年欠落を必要な地点だけに表示 |
| アクセシビリティ | 自動契約完了・実機継続 | native filter、構造化一覧、状態通知、focus復帰、200%／forced-colors、SwiftUIのAX名称とAnnouncement |
| 独立検証 | 完了 | NASA日食・月食、IOTA掲載例、Web／Swift共有fixture |
| 軽量性 | 継続監査 | 初期画面からイベント資産を分離し、共有resourceの同時読込を一つへ集約、地点・年ごとの直近3結果をLRU保持。Mac本番bundleから旧星表・検証fixture 13件を除外し、20 MiB上限を固定。年間予報・A→B→A再訪・メモリも計測し、各リリースのbundle実測は改善ログへ記録 |

予報年はUTC年ではなく、選択した観測地点のタイムゾーンにおける最大時刻の年で
区切る。年末年始を取りこぼさないようUTC範囲を前後2日広げて計算し、解いた後に
現地年で絞り込む。ただしDE442s端の光行時間reserveで現地年の一部を安全に
解けない1900年／2100年は、欠落時間を「現象なし」とせず画面へ明示する。

## 品質ゲート

- 元SPK hashが一致しない入力から生成しない。
- manifest外またはhash不一致のchunkを評価しない。
- 時刻の絶対値とJDをFloat32へ落とさない。
- 初期Webの既存予算を増やさない。
- イベントを開くまでイベント係数を取得しない。
- 予報失敗で星図を壊さない。
- 日食詳細から安全注意を隠さない。
- 両版の共有fixture差がイベント種別の許容差内に収まる。
- 平均月縁、EOP範囲外、地点精度不明を通常精度として表示しない。
- 端末測位の既知の水平精度を日食・掩蔽の境界判定へ伝え、都市・手入力では
  根拠のない精度値を作らない。
- 物理境界の不確実性と地平線可視性を別の状態として保持し、片方でもう片方を
  置き換えない。
- CIRS接平面の位置角規約、USNO照合値、未定義入力を両版で固定する。
- 現地年coverageの安全端とoffset別の欠落時間を共有fixtureから両版で再現する。
- 相対配置selectorの候補はsolverが返した接触と最大／最接近だけから作り、
  重複排除後にUTC順へ並べる。
- selectorだけでは星図時刻を変更せず、明示した「空に表示」操作だけが
  再生停止と日時変更を行う。星図時刻との一致判定は実際の観測日時から求め、
  再生再開後に古い接触図を現在時刻の図として見せない。
- 接触間の画面座標補間を物理計算として扱わない。
- 任意時刻は最初と最後の確定接触の範囲だけを許可し、最大／最接近しか
  確定できない境界事例では連続操作を無効にする。
- 固定画角の参照集合には区間端、全接触、最大／最接近と、現象種別ごとの
  60〜180秒間隔の物理sampleを含める。表示フレーム自体も指定UTCで再計算する。
- 年次予報LRUへ暦providerやEOP snapshotを保持するsampler closureを入れず、
  選択中イベント1件のsessionだけに保持する。選択変更時は旧sessionを取消・破棄し、
  時刻sample cacheは上限付きとする。
- 取消された待機側が共有resource読込を破棄せず、失敗後は再試行でき、
  A→B→Aの再訪で同じassetを再読込せず、直近3つの同一地点・年はsolver結果も
  再利用する。4つ目では最も古い結果を破棄する。

## 意図的に後段へ残すもの

- 小惑星掩蔽
- 月面地形プロファイルを使う接食、ベイリービーズ、sub-second接触
- 外部サーバーによるリアルタイム更新
- 通知、カレンダー、観測報告
